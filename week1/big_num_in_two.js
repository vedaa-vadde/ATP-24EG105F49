/**
 * Script to find the largest of two given numbers.
 */

const a = 10;
const b = 20;

if (a > b) {
  console.log(`${a} (a) is larger than ${b} (b).`);
} else if (b > a) {
  console.log(`${b} (b) is larger than ${a} (a).`);
} else {
  console.log("Both numbers are equal.");
}
